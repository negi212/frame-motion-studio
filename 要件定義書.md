# FRAME MOTION STUDIO 要件定義書

## 1. はじめに
### 1.1 目的
本書は、動画から動体を抽出し一定時間ごとの位置を1枚の静止画像に合成するデスクトップアプリケーション「FRAME MOTION STUDIO」の要件を定義する。

### 1.2 対象読者
企画者、設計者、開発者、テスト担当者

### 1.3 適用範囲
本アプリのデスクトップクライアント機能を対象とする。クラウド連携、動画編集のタイムライン機能、複数動画の同時合成は対象外。

---

## 2. システム概要
### 2.1 背景
スポーツ解析、ロボット軌跡確認などで、動きの経時的変化を1枚で俯瞰したい需要がある。動画をコマ送りで確認する手間を省き、残像を自動合成して可視化する。

### 2.2 システムの目的
- 入力動画から背景と動体を分離
- 指定間隔ごとに動体領域を抽出し、基準背景画像上に重畳合成
- 合成結果を画像ファイルとして保存
- GUIで直感的に操作可能とする

### 2.3 利用者像
- ロボコン、スポーツ、研究で軌跡を確認したい一般ユーザ
- PC操作に慣れているが映像処理の専門知識は不要と想定

---

## 3. 用語定義
| 用語 | 定義 |
|---|---|
| 入力動画 | 合成元となる動画ファイル（MP4/MOV/MKV/AVI等） |
| 出力画像 | 合成結果の静止画像（PNG/JPEG） |
| 動体 | 背景差分により検出された移動物体の領域 |
| 間隔 | 動体をサンプリングする時間間隔（秒） |
| 最小面積 | ノイズとして除外する輪郭の最小面積（ピクセル） |
| プレビュー | 処理前後の画像を画面内で確認する機能 |

---

## 4. システム構成・前提
- スタンドアロンなデスクトップアプリケーション
- オフラインで動作、外部サーバ依存なし
- Windows / macOS / Linux で動作すること（OSネイティブのファイル選択ダイアログを利用）

---

## 5. 機能要件

### 5.1 入力機能
#### F-01 動画選択
- ファイル選択ダイアログから入力動画を1件選択できること
- ドラッグ＆ドロップによる指定にも対応すること（可能な環境では）
- 選択後、ファイル名を表示すること
- 非対応形式が選択された場合は警告を表示すること

#### F-02 動画情報表示
- 選択した動画の以下を表示すること：解像度、フレームレート、総フレーム数、再生時間
- 取得できない場合は「--」等で表示し、処理を継続できること

### 5.2 出力機能
#### F-03 保存先指定
- 保存先ファイルパスを指定できること
- 未指定の場合は入力動画と同一ディレクトリに `元ファイル名_composite.png` を自動設定すること
- 保存先選択ダイアログで拡張子（PNG/JPEG）を選択できること
- 拡張子が未入力の場合は自動補完すること
- 上書き時は確認ダイアログを表示すること

#### F-04 画像保存
- 合成結果を静止画像として保存できること
- 保存先ディレクトリが存在しない場合は自動生成すること

### 5.3 合成処理
#### F-05 背景差分・動体抽出
- 背景モデルを生成し、各フレームとの差分から動体マスクを生成すること
- ノイズ除去のための形態学的処理（オープニング/クロージング）を行うこと
- 輪郭検出により動体領域を特定すること
- 面積が閾値未満の微小領域はノイズとして除外すること

#### F-06 時系列サンプリング
- 動画の再生時刻に基づき、指定した間隔（秒）ごとにサンプリングすること
- 間隔は可変とし、ユーザが指定できること

#### F-07 合成
- 動画の先頭フレームを背景画像として用いること
- サンプリングされた各時刻の動体画素のみを背景画像上に上書き合成すること
- 合成処理は中断可能なこと

#### F-08 パラメータ制御
- 以下のパラメータをユーザが変更できること：
  - 間隔（例: 0.05〜1.0秒）
  - 最小面積（例: 10〜2000 px）
  - 検出感度（背景差分の閾値）
  - 背景学習に用いる履歴フレーム数
- プリセットとして「きめ細かい / 標準 / あっさり」の3段階をワンクリックで適用できること
- 詳細パラメータは初期状態で非表示（折りたたみ）とし、「詳細設定」操作で表示/非表示を切り替えられること
- 各パラメータの現在値は数値で可視化すること

### 5.4 プレビュー機能
#### F-09 プレビュー表示
- 入力動画の先頭フレームをプレビュー表示すること
- 合成完了後に結果画像を同一領域に表示できること
- 「BEFORE / AFTER」ボタンで表示を切り替えられること
- 画像はアスペクト比を保持して領域内に収まるよう縮小表示すること

#### F-10 結果オープン
- 「開く」操作で出力画像をOS既定の画像ビューアで開けること

### 5.5 進捗・制御
#### F-11 進捗表示
- 処理中は進捗率（%）、現在処理時刻（秒）、プログレスバーを表示すること
- ヘッダ部のステータス表示（READY / PROCESSING / DONE / CANCELLED 等）を更新すること

#### F-12 実行制御
- 「合成する」ボタンで処理を開始できること
- 処理中は開始ボタンを無効化し、「キャンセル」ボタンを有効化すること
- キャンセル操作により処理を中断できること
- 未入力（動画未選択）で開始した場合は警告を表示し開始しないこと

#### F-13 エラーハンドリング
- 動画が開けない、FPS取得失敗、先頭フレーム読込失敗等のエラーはダイアログで通知すること
- エラー発生時は処理を中断し、ステータスをエラー表示に切り替えること

---

## 6. 画面要件

### 6.1 画面構成
- ヘッダ：アプリケーション名、副題、状態表示
- 左ペイン：
  - 動画選択カード（ドロップ領域、ファイル名、参照ボタン、動画情報）
  - 保存先カード（現在パス、変更ボタン）
  - 仕上がりカード（プリセット3択、詳細設定折りたたみ）
- 右ペイン：
  - プレビューカード（BEFORE/AFTER 切替、プレビュー領域、開くボタン）
  - 実行カード（ステータス、進捗バー、キャンセル/合成ボタン）
- フッタ：コピーライト等

### 6.2 UI/UX要件
- ダークテーマを基調とし、視認性と没入感を確保すること
- 文字サイズは可読性を確保し、ヘッダ副題等がクリッピングされないこと
- 主要操作（動画選択→合成）は2クリック以内で開始できること
- パラメータ等の情報量は初期表示では最小限とし、必要に応じて段階的に開示すること
- ホバー時の強調表示など、インタラクションに対する視覚的フィードバックを持つこと

---

## 7. 非機能要件

### 7.1 性能
- フルHD（1920x1080）30fps・10秒程度の動画を、一般的なノートPCで数十秒以内に処理できること
- プレビュー表示は選択後1秒以内に反映されること
- 処理中もUIはフリーズせず、進捗更新とキャンセル操作を受け付けること（非同期処理）

### 7.2 互換性
- 対応入力形式：MP4, AVI, MOV, MKV, M4V, WEBM（OSにインストールされたコーデックに依存するが、主要形式は動作すること）
- 対応出力形式：PNG, JPEG
- Windows / macOS / Linux で同一機能が利用できること
- 高DPIディスプレイで文字・UIがぼやけないこと

### 7.3 信頼性・可用性
- 処理中断時も入力動画や既存ファイルを破損しないこと
- アプリ終了時に処理中の場合は確認ダイアログを表示すること

### 7.4 保守性・拡張性
- 合成ロジックとUIは分離し、将来的なパラメータ追加や出力形式追加が容易であること

### 7.5 制約
- インターネット接続を必須としないこと
- GPUを必須としないこと

---

## 8. 入出力仕様

### 8.1 入力
- 入力動画ファイル：1ファイル、最大解像度 4K、最大数分程度を想定
- パラメータ：間隔、最小面積、感度、履歴（いずれも数値範囲内）

### 8.2 出力
- 出力画像：1ファイル、入力動画の解像度と同一、PNG/JPEG
- 出力先：ユーザ指定パス、未指定時は自動命名

---

## 9. 例外・エラー一覧
| 事象 | 表示 | 動作 |
|---|---|---|
| 動画未選択で実行 | 警告ダイアログ「動画を選択してください」 | 処理開始せず |
| 動画が開けない | エラーダイアログ | ステータスをERRORに |
| FPS取得不可 | エラーダイアログ | 同上 |
| 保存先上書き | 確認ダイアログ | ユーザ選択に従う |
| 処理中キャンセル | ステータスをCANCELLEDに | 部分的な出力ファイルは生成しない |

---

## 10. 受入基準
- [ ] 動画を選択し「合成する」を押すと、動体の残像が1枚に合成された画像が保存されること
- [ ] プリセット3種で仕上がりの粗密が変化すること
- [ ] 詳細設定を開閉でき、各パラメータ変更が合成結果に反映されること
- [ ] プレビューでBEFORE/AFTERを切り替えられること
- [ ] 進捗バーとキャンセルが機能すること
- [ ] 未選択状態での実行や不正ファイル選択時に適切な警告が出ること
- [ ] 生成画像を「開く」でOS標準ビューアで閲覧できること
- [ ] Windows/macOS/Linuxいずれか2OS以上で上記が再現すること

---

## 11. 今後の拡張候補（本要件外）
- 複数動画の一括処理
- 合成結果に対する透過度・色調整
- 動体検出方式の選択（背景差分以外のアルゴリズム）
- 動画プレビュー再生とタイムライン指定

---

*作成日: 2026-09-02 / バージョン: 1.0*
